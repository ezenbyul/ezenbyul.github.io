package Method;

public class PrinterEx {
	public static void main(String[] args) {
		Printer printer = new Printer();
		printer.Printerln(10);
		printer.Printerln(true);
		printer.Printerln(5.7);
		printer.Printerln("¹Ùº¸");
	}
}
